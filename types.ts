
export interface Company {
  name: string;
  description: string;
  website: string;
}

export interface GroundingSource {
  title: string;
  uri: string;
}

export interface GeolocationCoordinates {
  latitude: number;
  longitude: number;
}
